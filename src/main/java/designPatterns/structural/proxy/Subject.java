package designPatterns.structural.proxy;

//Subject is the interface that exposes the functionality available for use by the clients
public interface Subject {

    void someMethod() throws IllegalAccessException;
}
